{# templates/book/edit.html.twig #}

{% extends 'base.html.twig' %}


{% block body %}
    <h1>Edit Book</h1>
    <form method="post">
        {{ form_row(form) }}

        <button type="submit" class="btn btn-primary">Save</button>
    </form>
    <a href="{{ path('show') }}">Back to Book List</a>
{% endblock %}
